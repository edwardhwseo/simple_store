<h1>Products#index</h1>
<ol>
<% @products.each do |product| %>
    <li><%= link_to product.title, product %>, <strong>Category:</strong> <%= Category.find_by(id: product.category_id).name %></li>
<% end %>
</ol>
<p>Find me in app/views/products/index.html.erb</p>
